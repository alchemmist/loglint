linters:
  default: none
  enable:
    - loglint

  settings:
    custom:
      loglint:
        type: module
        description: Check log messaging in Go code.
        original-url: github.com/alchemmist/loglint
